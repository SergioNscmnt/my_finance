<% data = local_assigns[:data] || @dashboard_data %>
<section class="grid gap-4 grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
  <article class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm">
    <div class="flex items-center justify-between mb-3">
      <h3 class="text-sm font-semibold text-slate-900">Fluxo mensal</h3>
      <%= form_with url: dashboard_path, method: :get, local: true, class: "flex items-center gap-2 text-xs text-slate-600" do %>
        <label for="months" class="font-medium">Período</label>
        <%= select_tag :months,
              options_for_select([["1 mês",1],["3 meses",3],["6 meses",6],["9 meses",9],["1 ano",12],["2 anos",24],["3 anos",36]], data[:chart_months]),
              class: "slim-select w-28 rounded-md border border-slate-300 px-2 py-1 text-xs text-slate-700 bg-white focus:border-blue-500 focus:outline-none",
              onchange: "this.form.submit()" %>
      <% end %>
    </div>
    <div class="h-60">
      <canvas
        data-controller="chart"
        data-chart-type-value="line"
        data-chart-labels-value="<%= data[:chart_labels].to_json %>"
        data-chart-datasets-value='<%= [
          { label: "Receitas", data: data[:chart_income], borderColor: "#10b981", backgroundColor: "rgba(16,185,129,0.15)", tension: 0.3, fill: true },
          { label: "Despesas", data: data[:chart_expense], borderColor: "#f59e0b", backgroundColor: "rgba(245,158,11,0.15)", tension: 0.3, fill: true }
        ].to_json %>'
        data-privacy-target="value">
      </canvas>
    </div>
  </article>

  <article class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm">
    <div class="flex items-center justify-between mb-3">
      <h3 class="text-sm font-semibold text-slate-900">Composição</h3>
      <span class="text-xs text-slate-500">Receita vs Despesa</span>
    </div>
    <div class="h-60">
      <canvas
        data-controller="chart"
        data-chart-type-value="doughnut"
        data-chart-labels-value="<%= data[:pie_labels].to_json %>"
        data-chart-datasets-value='<%= [
          { label: "Composição", data: data[:pie_values], backgroundColor: ["#10b981", "#f59e0b"] }
        ].to_json %>'
        data-privacy-target="value">
      </canvas>
    </div>
  </article>

  <article class="rounded-xl border border-slate-200 bg-white p-4 shadow-sm">
    <div class="flex items-center justify-between mb-3">
      <h3 class="text-sm font-semibold text-slate-900">Top despesas</h3>
      <span class="text-xs text-slate-500">por categoria</span>
    </div>
    <div class="h-60">
      <canvas
        data-controller="chart"
        data-chart-type-value="bar"
        data-chart-labels-value="<%= data[:category_labels].to_json %>"
        data-chart-datasets-value='<%= [
          { label: "Despesa", data: data[:category_values], backgroundColor: "#f59e0b" }
        ].to_json %>'
        data-privacy-target="value">
      </canvas>
    </div>
  </article>
</section>
